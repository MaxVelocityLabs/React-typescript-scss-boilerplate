import { HomePageComponent } from './HomePage.component';

/**
 * HomePage container component that manages state and data for the homepage
 * @component
 * @returns JSX element containing the HomePageComponent
 * @example
 * ```tsx
 * <HomePageContainer />
 * ```
 */
export const HomePageContainer = () => {
  // Container logic can be added here for state management, data fetching, etc.
  // For now, it simply renders the HomePageComponent

  return <HomePageComponent />;
};
